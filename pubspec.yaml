name: music_player
description: Basic mp3 player.
publish_to: 'none'

version: 0.0.1

environment:
  sdk: '>=3.0.5 <4.0.0'
     
dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.10.4
  cupertino_icons: ^1.0.2
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/music.png